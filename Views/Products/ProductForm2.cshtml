@model OnlineStore.ViewModels.ProductFormViewModel

<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <title>Contact Us - Brand</title>
    <link rel="stylesheet" href="../../style/assets/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Montserrat:400,400i,700,700i,600,600i">
    <link rel="stylesheet" href="../../style/assets/fonts/font-awesome.min.css">
    <link rel="stylesheet" href="../../style/assets/fonts/simple-line-icons.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/baguettebox.js/1.10.0/baguetteBox.min.css">
    <link rel="stylesheet" href="../../style/assets/css/Navigation-with-Search.css">
    <link rel="stylesheet" href="../../style/assets/css/smoothproducts.css">
</head>

<body>
    <main class="page contact-us-page">
        <section class="clean-block clean-form dark">
            <div class="container">
                <div class="block-heading">
                    <h2 class="text-info" style="margin-top: 18px;">@Model.Title</h2>
                </div>
                @using (Html.BeginForm("save", "Products", FormMethod.Post, new {enctype = "multipart/form-data"}))
                {
                @Html.ValidationSummary(true,"Please fix the following errors.")
                <div class="form-group">
                    @Html.LabelFor(m=>m.File)
                    @Html.TextBoxFor(m => m.File, new { type = "file",@class="form-control"})
                    @Html.ValidationMessageFor(m => m.File)
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.Name)
                    @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m=>m.Name)
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.Price)
                    @Html.TextBoxFor(m => m.Price, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m=>m.Price)
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.description)
                    @Html.TextAreaFor(m => m.description, new { @class = "form-control" })
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.CategoryId)
                    @Html.DropDownListFor(m => m.CategoryId, new SelectList(Model.Categories, "Id", "Name")
                    , "Select Category", new { @class = "form-control" })
                    @Html.ValidationMessageFor(m=>m.CategoryId)
                </div>
                @Html.HiddenFor(m => m.Id)
                @Html.AntiForgeryToken()

                <div class="form-group"><button class="btn btn-primary btn-block" type="submit" value="Upload file">save</button></div>

                }


            </div>
        </section>
    </main>
    <script src="../../style/assets/js/jquery.min.js"></script>
    <script src="../../style/assets/bootstrap/js/bootstrap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/baguettebox.js/1.10.0/baguetteBox.min.js"></script>
    <script src="../../style/assets/js/smoothproducts.min.js"></script>
    <script src="../../style/assets/js/theme.js"></script>
    @Scripts.Render("~/bundles/jqueryval")

</body>
</html>

@section scripts
{
    @Scripts.Render("~/bundles/jqueryval")
}